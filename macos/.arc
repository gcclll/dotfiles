# ======================== A alias ========================

alias ainfo='echo "=== alias a ==="
echo "-- adb, adb, 浏览器URL管理命令"
echo "   adblt   = adb logcat -v time, adb 日志"
echo ""
echo "------------------------------------------------------------"
'

alias adblt="adb logcat -v time"

alias adduser="useradd"

# ubuntu
alias aptins="sudo apt install -y"
